1)
  a) A series of 0s and 1s that a computer processor can directly understand and execute.
  b) Instructions in binary that are executed directly by a computer’s CPU.
  c) The lowest level human readable language
  d) A low-level, platform-independent code generated by languages like Java. 
  It runs on a virtual machine rather than directly on the processor.

2) 
  a) The complete set of commands that a CPU can execute. Different processors have different instruction sets.
  b) Software that simulates a computer, translating instructions so programs can run on different processors.
  c) The part of the CPU that performs all arithmetic and logical operations.
  d) A CPU design philosophy that uses a small, simple set of instructions for faster performance.
  e) A virtual machine that runs Java byte code and translates it into processor-specific machine code.

3) Because compiled programs are converted into machine code specific to the Intel instruction set. 
Other processors, like Motorola, have different instruction sets, so the binary code won’t be understood.

4) Source code, compiler, byte code, JVM, executable

5) A general purpose programming language, like C++ or Java, are programming languages
that can be used to do a wide array of tasks

Exercise 6

a)  set of instructions that tells a computer what to do.
b) A formal language used to write programs, like Java or Python.
c) Software where programmers write and edit source code.
d) Software that combines an editor, compiler, debugger, and other tools to make programming easier.
e) A program that converts source code into machine code or byte code.
f) A program that executes source code line by line instead of compiling it first.